import React from 'react';
import ReactDOM from 'react-dom';
import App from '../../practice/src/App.js';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);